# Neon PostgreSQL 連線字串(Neon console → Connect → Connection string)
# 本機開發放 .env.local;Vercel 到專案 Settings → Environment Variables 設定
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Session JWT 簽章密鑰(openssl rand -base64 32);本機與 Vercel 各生成一份,不共用
SESSION_SECRET=change-me
